{
  "name": "ar-native-openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugins for AR smart glasses (Even Realities G2, Rokid, Meta Ray-Bans)",
  "license": "MIT",
  "workspaces": [
    "shared",
    "even-realities-g2",
    "rokid",
    "meta-raybans"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
